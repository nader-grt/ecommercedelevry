export default class AddProductToCategoryController
{
    constructor()
    {}

    async execute():Promise<any>
    {
        try {
            /**
              const category =
      await this.categoryRepository.findById(categoryId);

    if (!category) {
      throw new Error("Category not found");
    }

    const product =
      category.addProduct(productName);

    return product;
             */
        } catch (error) {
            
        }
    }
}